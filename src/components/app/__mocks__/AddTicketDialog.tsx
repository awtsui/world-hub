'use client';

export interface AddTicketDialogProps {
  event: Event;
}

export default function AddTicketDialog({ event }: AddTicketDialogProps) {
  return <div data-testid="add-ticket-dialog">mock</div>;
}
